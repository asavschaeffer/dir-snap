utils.py: Contains helper functions or constants that might be shared across modules. For example:

    Definition of the DEFAULT_IGNORE_PATTERNS.
    Maybe helper functions for parsing map formats if they get complex.
    Cross-platform utility functions if needed.